/// `services` contains all the individual services that make up the library. These are supposed to
/// be agnostic of any frontend that they get exposed through. In fact these can be thought of as
/// individual libraries.

pub mod auth;